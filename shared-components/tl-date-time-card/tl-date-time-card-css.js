import { css } from 'lit';

export const dateTimeStyles = css`
    .iconWrapper {
        max-width: 13.4em;
        min-height: 2.2em;
        background: var(--primary-green);
        border: 5px solid #1D0747;
        border-radius: 4em;
        display: flex;
        align-items: center;
        padding: 0 1em;
    }
    .iconWrapper span {
        margin: 0 1em;
    }
`;